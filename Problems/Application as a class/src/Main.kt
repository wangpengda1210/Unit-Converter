class Application(val name: String) {

    // write the run method here
    fun run(first: String, second: String, third: String) {
        println(name)
        println(first)
        println(second)
        println(third)
    }
}