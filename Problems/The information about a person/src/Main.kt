import java.util.Scanner

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    // put your code here
    print("${scanner.next().first()}. ${scanner.next()}, ${scanner.next()} years old")
}